#include <bits/stdc++.h>
using namespace std;

// unsigned long long int mhash[2000000];


int main(int argc, char const *argv[]){

	ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string n,m;
    cin>>n>>m;

    if(n == m) cout<<n<<'\n';
    else cout<<1<<'\n';
}